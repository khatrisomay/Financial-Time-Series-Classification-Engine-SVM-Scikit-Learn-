version: '3.8'

services:
  stock-svm-predictor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quantum_svm_app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped
